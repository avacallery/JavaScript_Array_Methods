//Includes
//takes a single argument and finds the instance in the array 
//returns true or false

const items = [1, 2, 3, 4, 5] 

const includesTwo = items.includes(7); 

console.log(includesTwo);
//returns false 